#include<stdio.h>
int main()
{
    char str[50],;
    int i=0;
    scanf("%s",&*ch);
    //gets(ch);
   /*while(ch!=NULL)
    {
        str[i]=ch;
        i++;
    }
    str[i]=NULL;*/
    printf("%s",*ch);
    return 0;
}
