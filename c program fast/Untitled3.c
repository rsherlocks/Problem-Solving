#include<stdio.h>
#include<limits.h>
int main()
{
    int a;
    char b;
    float c;
    double d;
    short
    printf("storage size for int data type:%\n",sizeof(a));
    printf("storage size for char data type:%\n",sizeof(b));
    printf("storage size for float data type:%\n",sizeof(c));
    printf("storage size for double data type:%\n",sizeof(d));
    return 0;
}
